name: GH Pages Redirect

on:
  workflow_dispatch:

jobs:
  gh_pages:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy 🎯
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs/gh_pages
