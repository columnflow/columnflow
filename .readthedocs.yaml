version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.9"
  jobs:
    pre_create_environment:
      - LAW_CONFIG_FILE="$PWD/docs/law.cfg" bash setup.sh ""


submodules:
  include: all
  recursive: true

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
